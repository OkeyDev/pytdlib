from .function import Function

from .message_source import MessageSource
from .ok import Ok
from typing import List

### FILE IS AUTOGENERATED. DO NO EDIT IT BY HAND ###

class ViewMessages(Function[Ok]):
    """
    Informs TDLib that messages are being viewed by the user. Sponsored messages must be marked as viewed only when the entire text of the message is shown on the screen (excluding the button). Many useful activities depend on whether the messages are currently being viewed or not (e.g., marking messages as read, incrementing a view counter, updating a view counter, removing deleted messages in supergroups and channels)

    :param chat_id: Chat identifier
    :param message_ids: The identifiers of the messages being viewed
    :param source: Source of the message view; pass null to guess the source based on chat open state
    :param force_read: Pass true to mark as read the specified messages even the chat is closed
    :return: :class:`Ok`
    """
    __slots__ = ("chat_id", "message_ids", "source", "force_read", "_extra", "_client_id", "_type")

    def __init__(self, chat_id: int = 0, message_ids: List[int] | None = None, source: MessageSource | None = None, force_read: bool = False):
        self.chat_id = chat_id
        self.message_ids = message_ids
        self.source = source
        self.force_read = force_read
        self._type = "viewMessages"